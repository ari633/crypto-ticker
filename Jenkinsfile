pipeline {
  agent any

  stages {
  
    
    stage('Test') {  
      echo "Testing ${TAG_NAME}"
    }  
    
    stage('Build') {
      steps {
        echo 'Building'
      }
    }  
    
    stage('Deploy') {
      echo 'Deploy Success'
    }
  }
}
